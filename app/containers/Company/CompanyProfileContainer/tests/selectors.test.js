// import { fromJS } from 'immutable';
// import { selectCompanyProfileContainerDomain } from '../selectors';

describe('selectCompanyProfileContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
