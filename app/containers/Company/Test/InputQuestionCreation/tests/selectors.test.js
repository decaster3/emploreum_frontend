// import { fromJS } from 'immutable';
// import { selectInputQuestionCreationDomain } from '../selectors';

describe('selectInputQuestionCreationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
