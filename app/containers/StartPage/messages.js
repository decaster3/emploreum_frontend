/*
 * StartPage Messages
 *
 * This contains all the text for the StartPage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.StartPage.header',
    defaultMessage: 'This is StartPage container !',
  },
});
