// import { fromJS } from 'immutable';
// import { selectEmployeeEnrollStateDomain } from '../selectors';

describe('selectEmployeeEnrollStateDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
