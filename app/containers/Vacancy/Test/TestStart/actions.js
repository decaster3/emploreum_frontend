/*
 *
 * TestStart actions
 *
 */

import {
  DEFAULT_ACTION,
} from './constants';

export function defaultAction() {
  return {
    type: DEFAULT_ACTION,
  };
}

export const startTest = (vacancyId) => {
// redirect to /employee/vacancy/vacancyId/test/1
}